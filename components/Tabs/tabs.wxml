<view class="tabs-title">
    <view class="title-item {{item.isActive?'active':''}}" wx:for="{{tabsData}}" wx:index="{{id}}" bind:tap="itemClick" data-index="{{index}}">
        {{item.title}}
    </view>
</view>
<view class="tabs-content">
    <slot></slot>
</view>